#!/usr/bin/env sh
# Fast gate (~5s). This is a pure library with no build step in the loop, so
# the whole deterministic gate fits comfortably inside a ~30s budget.
pnpm check
pnpm exec tsc --noEmit
pnpm test
pnpm test:harness
